apiVersion: v1
kind: Service
metadata:
  name: {{ include "rabbit-proxy.fullname" . }}
  labels:
    {{- include "rabbit-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "rabbit-proxy.selectorLabels" . | nindent 4 }}
